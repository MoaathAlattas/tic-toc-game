import { render } from '../../../lib/render.js'
import { UndoBtn } from '../undo_btn.js'

describe('ResetBtn', () => {
    test('test reset button', () => {
        expect(true).toBe(true);
    });
})