import { render } from '../../../lib/render.js'
import { ResetBtn } from '../reset_btn.js'

describe('ResetBtn', () => {
    test('test reset button', () => {
        expect(true).toBe(true);
    });
})